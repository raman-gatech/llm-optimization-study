version: 2
updates:
  # Benchmark and training requirement files are archived for reproducibility;
  # update them manually after rerunning the relevant environment validation.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    open-pull-requests-limit: 5
