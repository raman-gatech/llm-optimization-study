model_name: meta-llama/Llama-3.2-3B

output_dir: outputs/method0_ce
seed: 42
max_length: 512

batch_size: 2
eval_batch_size: 2
gradient_accumulation_steps: 4
num_workers: 2

lr: 2.0e-5
weight_decay: 0.01
warmup_ratio: 0.03
max_grad_norm: 1.0

max_steps: 1000
val_ratio: 0.05

log_every: 10
eval_every: 100
save_every: 250

eval_max_new_tokens: 192
eval_do_sample: false
eval_temperature: 1.0
eval_top_p: 1.0
eval_test_limit: null
